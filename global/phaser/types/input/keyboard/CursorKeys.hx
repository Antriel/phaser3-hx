package global.phaser.types.input.keyboard;

typedef CursorKeys = {
	/**
		A Key object mapping to the UP arrow key.
	**/
	var up : global.phaser.input.keyboard.Key;
	/**
		A Key object mapping to the DOWN arrow key.
	**/
	var down : global.phaser.input.keyboard.Key;
	/**
		A Key object mapping to the LEFT arrow key.
	**/
	var left : global.phaser.input.keyboard.Key;
	/**
		A Key object mapping to the RIGHT arrow key.
	**/
	var right : global.phaser.input.keyboard.Key;
	/**
		A Key object mapping to the SPACE BAR key.
	**/
	var space : global.phaser.input.keyboard.Key;
	/**
		A Key object mapping to the SHIFT key.
	**/
	var shift : global.phaser.input.keyboard.Key;
};